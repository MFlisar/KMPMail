<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name="com.michaelflisar.kmpmail.AppContextProvider"
            android:authorities="${applicationId}.appcontextprovider"
            android:exported="false"/>
    </application>

</manifest>

